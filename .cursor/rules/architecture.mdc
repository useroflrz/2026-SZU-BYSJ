## 架构与设计模式规范（Architecture.mdc）

- **目标**：在满足当前需求的前提下，让项目具备可维护性、可扩展性和可测试性，避免"堆功能"式开发。

### 1. 新需求评审与架构思考

- **必须先评审现有实现**
  - 在开发任何新功能前，先阅读相关模块（数据加载器、渲染器、分析算法等）的现有实现
  - 评估新需求与现有逻辑的关系：是扩展、替换，还是全新模块

- **强制设计模式/架构思考**
  - 若新需求会导致某个类超过 **300 行** 或者出现大量 `if/else`/`switch` 分支，必须考虑是否引入合适的设计模式，例如：
    - **策略模式（Strategy）**：不同数据格式的加载策略、不同可视域分析算法、不同渲染管线
    - **状态模式（State）**：应用状态（加载中、分析中、空闲）、相机状态（旋转、平移、缩放）
    - **观察者模式（Observer）/事件系统**：数据加载完成通知、分析进度更新、渲染事件
    - **命令模式（Command）**：撤销/重做操作、批量操作、操作历史记录
    - **工厂/抽象工厂（Factory）**：数据加载器创建、渲染对象创建、分析器创建
    - **组合（Composite）**：场景图节点、图层管理、UI 组件树
    - **适配器模式（Adapter）**：不同数据格式到内部数据结构的适配
  - 若逻辑跨模块/跨系统，需要考虑更高层次架构：
    - **模块化架构**：DataLoader、Renderer、Analysis、UI、Utils 等分模块
    - **MVC/MVP/MVVM**：对复杂 UI 使用，分离视图与逻辑
    - **管道模式（Pipeline）**：数据处理流程（加载 → 预处理 → 渲染）

### 2. 代码臃肿与重构原则

- **触发重构的典型信号**
  - 单个类承担多种职责，例如既处理数据加载、又处理渲染、又管理 UI
  - 任意类超过 **500 行** 且难以通过拆分方法改善可读性
  - 不同类出现大量重复代码（复制粘贴逻辑）
  - 新需求如果继续按当前写法，会让代码难以理解或难以测试

- **重构优先级**
  - 在开发新需求前，如已发现明显设计问题，应先做最小必要重构：
    - 提取独立类/接口
    - 通过接口/抽象类解耦依赖
    - 引入事件/消息中心代替直接引用

- **重构基本守则**
  - 保持对外行为不变（对已有功能的输入输出不改变）
  - 每次重构保持变更范围可控，优先处理最影响扩展性的部分
  - 重构后，更新相关 **文档**（见 `docs.mdc`）和 **注释**

### 3. 模块划分与命名规范

- **模块划分建议**
  - `Core`：核心通用代码（数学库、工具类、事件系统、服务定位等）
  - `GIS`：GIS 数据处理（数据加载、坐标转换、空间索引等）
  - `Rendering`：3D 渲染（渲染器、着色器、相机、场景管理等）
  - `Analysis`：分析功能（可视域分析、可见性计算、空间分析等）
  - `UI`：用户界面（窗口、控件、对话框、视图等）
  - `Utils`：工具函数、配置管理、日志系统等

- **命名要求**
  - 类名应体现职责：如 `DSMLoader`, `TerrainRenderer`, `ViewshedAnalyzer`, `MainWindowController`
  - 避免使用模糊命名：如 `Manager`, `Helper`, `Utils` 这类类名必须带有清晰前缀，如 `DataManager`, `RenderHelper`, `ConfigUtils`
  - 接口以 `I` 开头，抽象类以 `Base` 或 `Abstract` 开头

### 4. 依赖方向与解耦

- **依赖方向约束**
  - 高层模块不直接依赖低层具体实现，而是依赖接口或抽象
  - UI 不直接操作核心数据结构，优先通过中间层（例如 Controller 或 Service）
  - 数据加载层不依赖渲染层，通过事件或回调通知

- **常用解耦手段**
  - 事件总线/消息中心：例如 `EventBus`，避免双向引用
  - 服务定位器或依赖注入：统一获取全局服务（例如 `RenderService`, `DataService`）
  - 接口隔离：定义清晰的接口，避免接口过于庞大
  - 回调函数/函数对象：异步操作完成通知

### 5. 数据流与状态管理

- **数据流向**
  - 数据加载 → 数据预处理 → 数据结构化存储 → 渲染/分析
  - UI 操作 → 命令/事件 → 业务逻辑处理 → 数据更新 → UI 刷新

- **状态管理**
  - 应用状态应集中管理，避免状态分散在各处
  - 状态变更应通过统一接口，便于调试和日志记录
  - 支持状态序列化，便于保存和恢复

### 6. 对 Cursor 的要求

- **在生成代码前**，Cursor 必须：
  - 先判断当前需求属于哪个模块，并列出将影响的类/系统
  - 简要说明所采用的设计模式或架构思路（1～3 句）
  - 说明数据流向和处理流程

- **在代码生成中**，Cursor 应：
  - 避免在同一个类里混合多种职责
  - 主动拆分独立功能类或接口，并给出简短说明
  - 考虑性能影响（大数据集、实时渲染等）

- **在提交改动后**，Cursor 应：
  - 检查是否触发了重构条件，如触发则优先进行小步重构并更新文档
  - 列出潜在的性能瓶颈和改进建议
