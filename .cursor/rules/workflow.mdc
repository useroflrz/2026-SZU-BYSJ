---
alwaysApply: true
---

## 开发流程与协作规范（Workflow.mdc）

- **目标**：统一开发工作流，让每次需求迭代都"有文档、有设计、有实现、有验证"，方便人类开发者与 Cursor 协同。

### 1. 新需求处理流程

- **标准步骤**
  1. 明确需求：用 3～5 句话写清楚这个需求要达成的效果和验收标准
  2. 查阅现有文档：先在 `Docs` 与 `.cursor/rules/*.mdc` 中查找是否有相关设计
  3. 架构/模式选型：参考 `architecture.mdc` 决定是否需要新模块、新设计模式或重构
  4. 写简要设计文档：在对应文档目录中新增/更新设计说明
  5. 生成/编写代码：严格按照文档和规范实现
  6. 自测与验证：完成基础功能测试，记录已验证的要点（数据加载、渲染效果、分析结果等）

- **Cursor 在此流程中的职责**
  - 协助用户梳理需求并转化为 **结构化需求列表**（子任务）
  - 在回答中明确指出当前处于哪个步骤，并提示下一步建议
  - 对于复杂算法（如可视域分析），应主动建议先进行算法验证和性能测试

### 2. 任务拆分与 Todo 管理

- **任务拆分原则**
  - 将需求拆成 2～5 个可独立完成的小任务，例如：
    - 设计数据结构（GIS 数据内部表示、分析结果存储）
    - 搭建基础架构（接口、抽象类、数据流）
    - 实现核心逻辑（数据加载、渲染、分析算法）
    - 实现 UI 或表现层（界面、交互、可视化）
    - 编写/更新文档
    - 性能优化与测试
  - 对每个任务，写清输入、输出和完成条件

- **Cursor 行为要求**
  - 对复杂需求，Cursor 应主动给出任务拆分建议，并在执行过程中标记当前进行到哪一步
  - 对于涉及性能的任务（大数据加载、实时渲染），应单独列出性能测试任务

### 3. 代码评审与质量控制（轻量版）

- **自查清单**
  - 代码是否符合命名与目录规范？
  - 是否出现单个类/方法过于臃肿的情况（参考 `architecture.mdc` 的重构触发条件）？
  - 是否为公共接口/复杂逻辑写了必要注释或文档更新（参考 `docs.mdc`）？
  - 是否引入了不必要的耦合或全局状态？
  - 是否考虑了性能影响（大数据集、实时渲染、多线程安全）？
  - 是否处理了异常情况和错误输入？

- **Cursor 的检查点**
  - 在完成一批改动后，Cursor 应：
    - 简要列出本次改动的影响范围（哪些类/系统）
    - 指出潜在技术债（若有）并给出后续改进建议
    - 提醒需要测试的场景（如大数据加载、边界情况）

### 4. 测试与验证

- **功能测试**
  - 数据加载：测试不同格式、不同大小的数据文件
  - 渲染效果：验证渲染正确性、性能表现
  - 分析功能：验证分析结果正确性（可视域分析、可见性计算）
  - UI 交互：验证操作流畅性、响应速度

- **性能测试**
  - 大数据集加载时间、内存占用
  - 渲染帧率、GPU 使用率
  - 分析算法执行时间、CPU 使用率
  - 多线程并发安全性

- **边界情况测试**
  - 空数据、无效数据、损坏文件
  - 超大文件、极端坐标值
  - 内存不足、资源耗尽情况

### 5. 提交与版本记录（如使用版本控制）

- **提交信息建议**
  - 使用简洁的英文或中文摘要，如：
    - `feat: 新增 DSM 数据加载器`
    - `feat: 实现可视域分析算法`
    - `refactor: 重构渲染管线`
    - `perf: 优化大数据集加载性能`
    - `docs: 更新数据格式文档`
  - 提交前确保本地能正常编译、基本功能能正常运行

- **变更记录**
  - 对较大版本或核心系统变更，在 `Docs/Changelogs` 中记录摘要和迁移说明
  - 记录 API 变更、数据格式变更、配置变更等

### 6. 面向未来维护的思考

- **每次改动需考虑的问题**
  - 若半年后回来看这段代码/文档，是否还能快速理解？
  - 如果要做同类新功能（新数据格式支持、新分析算法），是否可以在当前架构下快速实现？
  - 是否有可以抽象成通用模块的部分，减少重复劳动？
  - 性能是否满足实际使用场景（大数据集、实时交互）？

- **Cursor 的长期视角**
  - 不只满足当前一次性请求，还要在回答中适当提醒：
    - 哪些地方未来可能成为扩展点（新数据格式、新分析算法）
    - 哪些设计是为了后续某类需求预留的
    - 性能瓶颈可能出现在哪里，如何优化

### 7. 性能优化流程

- **性能问题识别**
  - 使用性能分析工具（Profiler）定位瓶颈
  - 关注：CPU 使用率、内存占用、GPU 使用率、I/O 操作

- **优化策略**
  - 算法优化：选择更高效的算法，降低复杂度
  - 数据结构优化：使用更合适的数据结构，减少内存占用
  - 并行化：利用多线程、GPU 计算加速
  - 缓存机制：缓存计算结果，避免重复计算
  - 延迟加载：按需加载数据，减少初始加载时间

- **优化验证**
  - 优化前后性能对比
  - 确保优化不影响功能正确性
  - 记录优化效果和改进建议

### 8. Cesium 版本与 API 约定

- **版本固定**
  - 当前项目前端三维地图统一使用 `cesium@1.99.0`。
- **API 参考要求**
  - 编写或修改与 Cesium 相关的代码时，必须以 **Cesium 1.99.0 官方文档/API** 为准进行查阅与实现。
  - 避免直接复制使用更高版本 Cesium 才提供的 API；如需引入新特性，需先评估与 1.99.0 的兼容性与替代实现。
