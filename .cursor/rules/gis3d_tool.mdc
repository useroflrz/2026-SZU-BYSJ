---
alwaysApply: true
---
## 3D GIS 渲染工具开发规范（gis3d_tool.mdc）
> 当前工程根目录：`E:/biyesheji`（3D GIS 数据可视化与分析工具）

- **目标**：确保项目结构清晰、模块划分合理、代码风格统一，支持 GIS 数据（DSM、网格、点云等）的加载、渲染和可视域分析，方便 Cursor 精准生成和修改代码。

### 1. 工程结构与命名

- **目录结构建议**
  - `src/Core`：核心渲染引擎、数学库、工具类
  - `src/GIS`：GIS 数据加载与处理模块
    - `src/GIS/DataLoader`：数据加载器（DSM、网格、点云等）
    - `src/GIS/DataProcessor`：数据预处理与转换
    - `src/GIS/CoordinateSystem`：坐标系统转换
  - `src/Rendering`：3D 渲染相关
    - `src/Rendering/Renderer`：渲染器封装
    - `src/Rendering/Shader`：着色器管理
    - `src/Rendering/Camera`：相机控制
  - `src/Analysis`：分析功能模块
    - `src/Analysis/Viewshed`：可视域分析
    - `src/Analysis/Visibility`：可见性计算
  - `src/UI`：用户界面
    - `src/UI/MainWindow`：主窗口
    - `src/UI/Controls`：控件组件
    - `src/UI/Dialogs`：对话框
  - `src/Utils`：工具函数、配置管理
  - `data`：测试数据、示例数据
  - `resources`：资源文件（图标、配置文件等）

- **命名规则**
  - 类名采用 PascalCase，体现功能职责，如 `DSMLoader`, `ViewshedAnalyzer`, `TerrainRenderer`
  - 文件名必须与类名保持一致
  - 私有成员变量使用 `m_` 前缀或 `_` 后缀，如 `m_terrainData` 或 `terrainData_`
  - 常量使用全大写，如 `MAX_HEIGHT_VALUE`, `DEFAULT_GRID_SIZE`
  - 接口以 `I` 开头，如 `IDataLoader`, `IRenderable`

### 2. 核心模块设计规范

- **GIS 数据加载**
  - 所有数据加载器应实现统一接口 `IDataLoader<T>`，支持异步加载
  - 数据加载应支持进度回调，便于 UI 显示加载状态
  - 大文件（如大型 DSM、点云）应支持分块加载和流式处理
  - 数据格式转换应在加载阶段完成，统一转换为内部数据结构

- **3D 渲染**
  - 渲染器应封装底层图形 API（OpenGL/DirectX/Vulkan），提供统一接口
  - 使用场景图（Scene Graph）管理渲染对象层级
  - 支持 LOD（细节层次）以优化大场景性能
  - 着色器资源应统一管理，避免重复加载

- **可视域分析**
  - 可视域计算应支持多线程，避免阻塞 UI
  - 算法应模块化，支持不同算法实现（如射线法、栅格法）
  - 结果应可序列化保存，支持导出为常见格式（GeoJSON、Shapefile 等）

### 3. 性能优化规范

- **内存管理**
  - 大数据集应使用智能指针或 RAII 管理内存
  - 及时释放不再使用的数据，避免内存泄漏
  - 使用对象池管理频繁创建/销毁的对象（如渲染批次）

- **渲染性能**
  - 使用视锥剔除（Frustum Culling）减少不可见对象渲染
  - 使用遮挡剔除（Occlusion Culling）优化复杂场景
  - 批量渲染相同材质的对象，减少 Draw Call
  - 使用 GPU 计算进行大规模并行计算（如可视域分析）

- **数据优化**
  - 大型网格数据应支持压缩存储
  - 使用空间索引（如四叉树、八叉树）加速空间查询
  - 支持数据预处理和缓存机制

### 4. 坐标系统与数据格式

- **坐标系统**
  - 内部统一使用世界坐标系（World Coordinate System）
  - 支持常见 GIS 坐标系统转换（WGS84、UTM、投影坐标系等）
  - 坐标转换应封装为独立模块，便于维护和扩展

- **数据格式支持**
  - DSM：支持常见格式（GeoTIFF、ASCII Grid、DEM 等）
  - 网格：支持 OBJ、PLY、STL 等 3D 模型格式
  - 点云：支持 LAS、LAZ、PCD 等格式
  - 矢量数据：支持 Shapefile、GeoJSON、KML 等

### 5. UI 与交互

- **主窗口设计**
  - 使用现代 UI 框架（如 Qt、WPF、ImGui 等）
  - 主窗口应包含：3D 视图区、工具栏、属性面板、图层管理
  - 支持多视图（顶视图、侧视图、3D 视图）

- **交互控制**
  - 相机控制：支持鼠标拖拽旋转、缩放、平移
  - 支持键盘快捷键操作
  - 提供工具栏按钮和右键菜单
  - 支持撤销/重做操作

- **数据管理**
  - 提供图层管理功能，支持显示/隐藏、透明度调整
  - 支持数据导入/导出功能
  - 提供属性查询和标注功能

### 6. 配置与扩展性

- **配置文件**
  - 使用 JSON 或 XML 存储应用配置（窗口大小、默认路径等）
  - 支持用户自定义配置（快捷键、渲染参数等）
  - 配置应可导入/导出

- **插件系统（可选）**
  - 核心功能与扩展功能分离
  - 支持插件接口，便于后续扩展新功能
  - 插件应独立编译，动态加载

### 7. 面向 Cursor 的特殊要求

- **代码生成时**
  - Cursor 生成类时，应：
    - 明确说明该类的职责和所属模块
    - 指出需要依赖的第三方库或框架
    - 说明数据流向和处理流程
  - 给出推荐的测试数据路径和示例

- **关联其他规则**
  - 如新建功能模块（例如新的分析算法、数据格式支持），Cursor 应先参考：
    - `architecture.mdc` 决定整体架构与设计模式
    - `docs.mdc` 先写简要设计文档再生成代码
